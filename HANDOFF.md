# 한끼짝꿍 — 프로젝트 인수인계 (Claude Code용)

이 문서를 먼저 읽고 작업을 시작해줘. 함께 있는 `한끼짝꿍_프로토타입_v35.html`이 현재까지의 완성 프로토타입이야. 이걸 기반으로 (1) 코드를 깔끔한 구조로 리팩터링하고 (2) 실제 배포 가능한 앱으로 만들고 싶어.

- 공모전: **나는 Solo AI 2026** (부산정보산업진흥원 주최) · 마감 **7/31(금) 14:00**
- 공모전 링크: https://contestkorea.com/sub/view.php?Txt_bcode=030310001&int_gbn=1&str_no=202607020009

## 0. 한 줄 요약
"냉장고에 있는 재료만 알려주면 AI가 지금 만들 수 있는 메뉴를 추천해주는" 요리 초보(2030 자취생)용 모바일 웹앱. 마스코트는 계란요정 "한끼요정". 현재 단일 HTML 파일(약 1MB) 프로토타입 상태이고, Claude API를 실제로 호출해서 작동함.

- 앱 이름: **한끼짝꿍**
- 캐릭터: **한끼요정** (계란 캐릭터 — 흰자 몸통 + 노른자 얼굴)
- 브랜드 슬로건: **"없어도 괜찮아, 있는 걸로 충분해요."**
- 타겟: 요리를 잘 못 하는 2030 1인가구
- 공모전 출품작

## 1. 지금 무슨 일을 해야 하나 (목표)

### 목표 A. 리팩터링 (단일 HTML → 제대로 된 구조)
현재는 모든 게 하나의 HTML 파일에 들어있음 (HTML + CSS + JS + base64 이미지). 이걸:
- 컴포넌트/모듈 단위로 분리
- 추천 스택: React (Vite) + TypeScript, 또는 Next.js (아래 "기술 스택" 참고)
- base64로 박혀있는 이미지들(캐릭터 4종, 아이콘 3종)을 실제 asset 파일로 추출
- CSS는 현재 순수 CSS 변수 기반 → Tailwind 또는 CSS Module로 정리

### 목표 B. 실제 배포 (백엔드 붙이기)
현재 치명적 한계: Claude API 호출이 claude.ai 아티팩트 프리뷰 안에서만 작동함. 로컬에서 HTML 열면 API 안 됨 (API 키를 클라이언트에 노출할 수 없어서 프록시가 필요). 그래서:
- Supabase (또는 Vercel Serverless Function)로 백엔드 프록시를 만들어 Claude API 호출을 서버에서 처리
- API 키는 서버 환경변수로 숨김
- Vercel로 프론트 배포
- (참고: 개발자 Supabase 계정 있고 사용 경험 있음)

### 목표 C. 남은 기능 작업 (홈 개편 등)
아래 "미완성/TODO" 섹션 참고. 홈 화면 정보구조 개선이 제일 우선.

## 2. 현재 완성된 기능 (v35 기준)
프로토타입은 완성도 높음. 아래 기능들이 이미 다 작동함.

**화면 구조 (SPA, 화면 전환은 `.screen` div를 토글)**
- 홈 (`#home`): 캐릭터 히어로 + 슬로건 + 시작 버튼 2개 + 인기 레시피 캐러셀 + 하단 탭바
- 재료로 시작 (`#s-ing`): 재료 칩 입력 → AI가 만들 수 있는 메뉴 추천
- 요리명으로 시작 (`#s-dish`): 요리명 입력 → 만들 수 있는지 판정. 인기 요리 그리드 + 필터/정렬
- 결과 카드: 완성도 %, 정보 바(시간/난이도/설거지), AI 추천 이유 카드, 알레르기 경고, 있는/없는 재료, 만드는 법 버튼
- 쿡 모드 (`#s-cook`): 전체화면 단계별 요리 안내 + 재료 준비 화면 + SVG 일러스트 + "도와줘요!" SOS 버튼(5가지 상황별 해결책)
- 마이페이지 (`#s-me`): 로그인, 알레르기/식성 설정(대체재 추천 로직 포함), 최근 만든 요리, 오늘의 기록(통계), 레시피북(커뮤니티 맛보기)

**핵심 로직**
- 재료 → 메뉴 추천 / 메뉴 → 실현가능성 판정 두 흐름
- AI 반응: 재료 넣으면 요정이 실시간 반응 ("😊 계란 있으시네요! 만들 수 있는 메뉴가 N개 있어요")
- AI 추천 이유: 결과마다 왜 추천했는지 카드로 설명
- 알레르기 개인화: 로그인 시 알레르기 재료 감지 + 대체재 제안 (예: 우유→오트밀크/두유)
- **데모 폴백 (중요!)**: API가 안 될 때를 대비해 17개 요리 데모 데이터 내장. 단일 재료(감자/두부/양파 등)도 대응. 발표/시연 때 이게 생명줄이라 반드시 유지해야 함.

**디자인 시스템 (그대로 유지할 것)**
- 색상 팔레트 (계란 테마):
  - `--ink:#4A4038` (본문), `--ink-soft:#9A8F7E` (보조)
  - `--paper:#FFFDF7`, `--paper-dk:#FDF6E9` (흰자)
  - `--orange:#FFD23E` (노른자), `--orange-dk:#F5B301`
  - `--olive:#C9A227`, `--olive-bg:#FFF3C4`, `--blush:#F4A9A0`, `--line:#EFE6CC`
  - 인기 뱃지 초록: `#7BA85A`
- 폰트: 제목=Paperlogy(페이퍼로지, ExtraBold), 본문=Pretendard. (Paperlogy는 큰 제목에만, 글자 뭉침 방지 위해 letter-spacing 살짝 +)
- 앱 너비: 390px (아이폰 기준)
- 캐릭터 표시 규칙: 흰색 캐릭터가 밝은 배경에서 안 보이는 문제 → 캐릭터 뒤에 2겹 방사형 그라데이션 원(안쪽 흰색 코어 + 바깥 노른자 후광)을 깔아서 해결. **캐릭터 자체는 절대 수정하지 말고 배경 후광으로 가시성 확보.**

## 3. 미완성 / TODO (우선순위 순)

### 🔴 우선순위 1 — 홈 화면 개편 (정보구조)
현재 홈이 "포스터" 느낌. "실제 서비스" 느낌으로 개선 필요.
1. **슬로건 + 서비스 설명 분리** — 현재 "있는 재료로 충분해요"(슬로건)만 있음. 그 아래 서비스 설명 한 줄 추가: "냉장고 속 재료만 선택하면 AI가 지금 만들 수 있는 레시피를 추천해드려요."
2. **🥚 한끼요정 추천 카드 (핵심)** — 캐릭터 아래 작은 카드. 예: "오늘 냉장고에 계란이 있다면? → 계란볶음밥 / 오믈렛 / 계란국". AI가 추천하는 앱임을 3초 안에 이해시키는 요소.
3. **"어떻게 시작할까요?" + 시작 의미 부여** — 시작 버튼 2개 위에 문구 추가하고, 버튼에 ①메뉴 아직 못 정함 / ②메뉴 정함 의미 부여.
4. **Hero 압축** — 캐릭터 10~15% 축소, 후광 축소, 세로 여백 줄여 아래 콘텐츠 더 노출.
5. **냉장고 활용 팁 카드** — "💡 시들기 직전 채소는 볶음으로" 같은 팁 카드 하나.
6. (주의) "최근 본 레시피", "제철 재료"는 데이터/로직 없이 넣으면 빈 껍데기가 되니, 백엔드 붙인 후에 추가.

### 🔴 우선순위 2 — 결과 화면 "🤖 AI 분석" 카드
결과 화면에 AI가 일한 느낌을 주는 카드 추가:
```
🤖 AI 분석
· 냉장고 재료 활용도 92%
· 조리시간 15분
· 설거지 적음
· 초보 성공률 높음
```
(현재 "추천 이유" 카드는 있지만, 이것과 별개로 "분석" 느낌의 데이터 카드가 하나 더 있으면 좋음.)

### 🟡 우선순위 3 — 디테일 개선
- 인기 레시피에 기준 명시: "이번 주 사용자들이 많이 저장한 레시피" 같은 문구
- 난이도 별점 표시: ⭐초보 / ⭐⭐보통 / ⭐⭐⭐어려움
- 레시피북을 "저장한 레시피" vs "내가 등록한 레시피" 로 분리
- 홈에서 🤖 AI 추천 vs 🔥 인기 레시피의 역할 분리 (전자=개인화, 후자=사회적 증거)

### 🟢 우선순위 4 — 백엔드/배포 (목표 B)
- Supabase 또는 Vercel Function으로 Claude API 프록시
- 사용자 인증 (Supabase Auth) — 로그인/레시피북/통계를 실제 DB에 저장
- 레시피 커뮤니티 실제 구현 (등록/저장/랭킹) — 지금은 UI만 있는 맛보기

## 4. AI(Claude) API 연동 방법

**현재 (프로토타입)**
클라이언트에서 직접 `https://api.anthropic.com/v1/messages`로 fetch. claude.ai 아티팩트 안에서만 작동 (프록시가 자동 처리됨). 모델은 `claude-sonnet-4-6` 사용, max_tokens 1000.

**배포 시 (해야 할 것)**
클라이언트가 직접 Anthropic API를 부르면 API 키가 노출되므로 절대 안 됨. 서버 프록시 필요:
```
[프론트] → [Supabase Edge Function 또는 Vercel Function] → [Anthropic API]
                    ↑ 여기 API 키를 환경변수로 숨김
```
- Anthropic API 키는 `.env`에 넣고 **절대 커밋하지 않기**
- 프록시 함수는 프론트에서 받은 프롬프트를 Anthropic으로 넘기고 응답만 반환

**프롬프트 (프로토타입에서 검증됨, 그대로 재사용 가능)**
재료 → 메뉴 추천 (JSON만 출력하도록):
```
너는 요리 초보를 돕는 다정한 AI야. JSON만 출력해(설명·마크다운 없이).
형식:{"mode":"ing","suggestions":[{"dish":"요리 이름","percent":0~100 정수,"summary":"한 줄 소개","missingBrief":"부족하면 짧게, 없으면 '지금 바로 가능'"}]}
가진 재료로 지금/거의 만들 수 있는 쉬운 요리를 완성도 높은 순 3~4개.
재료가 1~2개뿐이어도 반드시 그 재료만으로 할 수 있는 가장 간단한 요리를 알려줘...
가진 재료:${재료목록}
```
(요리명 판정 프롬프트도 HTML 안에 있으니 참고해서 추출할 것.)

## 5. 기술 스택 제안 (논의 후 결정)
- 프론트: React + Vite + TypeScript (가볍고 배포 쉬움). Next.js도 가능하나 이 규모엔 Vite로 충분.
- 스타일: Tailwind CSS + 위의 색상 팔레트를 CSS 변수/테마로 이식.
- 백엔드: Supabase (Auth + DB + Edge Functions 한 번에 해결) 권장. API 프록시는 Edge Function.
- 배포: Vercel (프론트) + Supabase (백엔드).
- 상태관리: 이 규모면 Context/zustand 정도로 충분.

## 6. 작업 순서 제안
1. 먼저 v35 HTML을 분석해서 화면/컴포넌트 구조를 파악해줘.
2. Vite + React + TS 프로젝트 골격 생성.
3. 디자인 토큰(색/폰트) 이식 → 공통 컴포넌트부터.
4. base64 이미지 7종(캐릭터 happy/wave/sad/sleep, 아이콘 back/home/me)을 asset으로 추출.
5. 화면별 컴포넌트 이관 (홈 → 재료입력 → 결과 → 쿡모드 → 마이페이지 순).
6. 데모 폴백 데이터는 반드시 보존. (17개 요리 + 단일재료 매핑)
7. 그다음 홈 개편(TODO 1) → AI 분석 카드(TODO 2) → 백엔드.

## 7. 절대 지켜야 할 것 (중요)
- ✅ **데모 폴백 유지** — API 안 될 때 시연용. 이거 없으면 발표 때 망함.
- ✅ **색상/캐릭터/톤 유지** — 브랜드 정체성. 정보구조·UX만 개선.
- ✅ 캐릭터는 배경 후광으로 가시성 확보 (캐릭터 자체 수정 금지).
- ✅ **가짜 데이터 금지** — "3만명이 찾음" 같은 허구 숫자 넣지 말 것 (심사위원이 잡아냄). 통계는 실제 카운트되게.
- ✅ 한국어 UI — 모든 문구 한국어.
- ✅ **API 키 절대 커밋 금지.**

## 8. 배경 지식 (공모전)
- 마감: 7/31(금) 14:00 — 프로토타입은 사실상 완성. 코드 이관/배포는 마감 후에 진행해도 됨.
- 제출물: 프로토타입 + 출품작 설명서(3페이지) + 시연영상(3분).
- 즉, 당장은 **시연 가능한 상태 유지가 최우선**이고, 코드 이관/배포는 공모전 이후 실서비스화 단계.

**첨부 파일:** `한끼짝꿍_프로토타입_v35.html` (현재 완성 프로토타입, 이걸 기준으로 작업)

---

## 9. 작업 로그 / 현재 진행 상황 (2026-07-29) — ⚠️ 이어서 작업 시 먼저 읽기

### 방금 한 일: 레시피 DB 확장 (엑셀 4종 → v35에 주입)
자취 초보가 **메뉴를 검색해도 안 나오는 문제**를 줄이려고, 받은 엑셀 레시피를 `한끼짝꿍_프로토타입_v35.html`에 통합함.

- 엑셀 4파일 분석 결과: `V1_60개`(주재료·태그·검색키워드, 조리순서 없음) + `V1-1/2/3`(재료분량·조리순서·요리팁). **id는 안 맞고 메뉴명 기준 병합 = 총 77개 고유 요리.**
- 이번엔 **조리순서가 있는 상세본 60개**를 정규화해서 주입함 (겹치는 43개는 V60 주재료로 보강). 정규화 데이터는 저장소의 **`recipes.json`**(읽기 좋은 형태) / **`recipes.gen.js`**(`const RECIPES=[...]`, HTML에 인라인된 것과 동일).
- HTML에 주입한 것 (모두 `<script>` 안, 검색으로 `mergeRecipes`/`renderRecipe`/`const RECIPES` 찾기):
  1. `const RECIPES=[...]` (60개) + `mergeRecipes()` 어댑터 → 각 레시피를 `DEMO`(이름검색 폴백)와 `POPULAR`(둘러보기 그리드)에 병합. **중복 이름은 기존 수제 DEMO 유지.**
  2. 둘러보기 필터를 **한식/양식/일식 → 카테고리(전체/밥/면/국·찌개/반찬/메인/샐러드/간식/빠른순)**로 개편 (`FILTERS`, `popList()`).
  3. 이름검색 결과용 **`renderRecipe()`** 신설 — 가짜 완성도% 대신 **"레시피 준비됨" + 필요한 재료 전체 + 요리팁/대체재 + 만드는 법 보기**. `renderDish()`가 `o.recipeOnly`면 여기로 위임.
  4. 관련 CSS(`.need-list`, `.tip-card`, `.rr-badge` 등)는 `</style>` 앞에 추가.
- 백엔드/AI 로직은 **안 건드림.** 로컬에서 열면(=claude.ai 밖) fetch 실패 → 기존 데모 폴백 경로로 `DEMO` 렌더 → 새 60개도 검색됨(확인 완료). 온라인 AI 경로는 그대로.
- 원본 백업: `한끼짝꿍_프로토타입_v35.html.bak` (레시피 주입 전, gitignore됨). 직전 커밋(204bcef)에도 원본 있음.

### ✅ [해결됨 2026-07-29] 엑셀 데이터 품질 문제 — B안 채택, 34개 전부 채움
아래 미해결이던 placeholder 34개를 **B안(전부 실제 레시피로 작성)**으로 처리 완료. `recipes.json`·`recipes.gen.js`·HTML(`const RECIPES`) 모두 반영. placeholder 잔재 0개 확인. 자세한 내용은 이 섹션 맨 아래 **"### 🟢 [2026-07-29 이어서] 완성도 작업 로그"** 참고.

<details><summary>(과거 기록) 당시 미해결 상태</summary>

상세본 60개 중 **실제로 제대로 채워진 건 약 26개뿐**, 나머지 **34개는 자리표시자(placeholder)**였음:
- 예) "잡채" → 재료 `주재료 적당량`, 조리법 `1.재료를 손질한다 2.잡채에 맞게 조리한다 3.간을 맞춘다 4.완성한다`, 요리팁 `재료 상태에 따라 시간을 조절하세요`.
- **placeholder 34개**: 칼국수, 냉면, 새우볶음밥, 닭갈비덮밥, 소고기덮밥, 카레라이스, 짜장밥, 감자조림, 멸치볶음, 시금치나물, 애호박볶음, 오이무침, 브로콜리무침, 닭가슴살샐러드, 두부샐러드, 참치샐러드, 웨지감자, 오징어볶음, 순두부찌개, 청국장, 갈비탕, 닭곰탕, 육개장, 부대찌개, 비빔냉면, 물냉면, 잡채, 월남쌈, 유부초밥, 김밥, 오코노미야키, 타코라이스, 샥슈카, 프렌치토스트.
- **지금은 60개 다 들어가 있어서**, 위 34개를 검색하면 "주재료 적당량"이 보임 → 가짜처럼 보이는 상태. 데모/제출 전 반드시 처리해야 함.

**선택지 (사용자에게 A/B/C 질문 던진 상태, 답 대기 중):**
- **A. 진짜 26개만 남기기** — 34개 placeholder 제거(그리드+DEMO). 안전·정직. (검색 요리 ≈ 기존17+26)
- **B. 34개도 실제 레시피로 채워 60개 전부** (추천) — 잡채·김밥·불고기·육개장 등 표준 요리라 정확한 재료·조리법 작성 가능. `recipes.json`의 해당 항목 `ingredients`/`steps`/`tip`/`sub`/`reason`을 실제 내용으로 채우고 → `recipes.gen.js` 재생성 → HTML의 `const RECIPES=[...]` 한 줄을 교체(정규식으로 치환)하면 됨. ("가짜 데이터 금지"는 허구 통계 얘기고, 정확한 레시피 작성은 해당 안 됨.)
- **C. 26개 + 자주 찾는 것 일부만 채우기.**

</details>

### 🟢 [2026-07-29 이어서] 완성도 작업 로그 — ⚠️ 다음에 이어서 할 때 먼저 읽기
공모전 제출용 "실제 서비스처럼 보이는 완성도" 목표로, 코어 가치(**대체재 안내** + **조리 과정 동행**)를 강화하는 작업을 진행함. **전부 HTML(코드) 변경이라 `recipes.json`/`recipes.gen.js`는 레시피 데이터만 담고 그대로임.** 백엔드/AI 로직 안 건드림 → 로컬 데모 폴백으로 전 기능 확인 완료(콘솔 에러 0).

**한 일 (검색 앵커):**
1. **레시피 placeholder 34개 → 실제 레시피로 전부 작성** (B안). `recipes.json` 60개 모두 재료·조리순서·팁·대체재 채움 → `recipes.gen.js` 재생성 → HTML `const RECIPES=[...]` 한 줄 교체. 백업 `_prefill_backup.html`(gitignore).
2. **🔄 대체재 기능 전면화 (코어 가치)** — 지식베이스 `const SUBS=[...]`(80개, 치킨스톡·굴소스·맛술·파마산 등) + `subFor()` + `window.toggleSub()`. `renderRecipe()`의 재료 목록에서 각 재료에 **"없어요" 버튼** → 짝꿍이 대체안 표시. (검색: `SUBS`, `subFor`, `ni-no`, `ni-sub`)
3. **🤖 AI 분석 카드** — `buildAnalysisCard(o,mode)` 신설. 냉장고 활용도(%바)·조리시간·설거지·초보 성공률·**대체 가능 N가지**(코어 가치 데이터화). `renderDish`/`renderRecipe` 양쪽에 삽입. 모든 값 실제 값(가짜 없음). (검색: `buildAnalysisCard`, `ai-card`)
4. **홈 = 라이브 AI 미리보기 (핵심, "AI가 작동한다" 실감)** — 첫 화면에서 재료 칩 탭 → 짝꿍이 "🤔 찾는 중…"(480ms) → 그 자리에서 실제 추천 인라인 표시(계란→계란후라이 95%·계란말이 88%·계란찜 82%). `ING_DISH`/`demoSuggestFor()` 실제 데이터 재사용. CTA "이 재료로 더 찾아보기 →"는 `startDemoSel()`로 재료검색 진입. (검색: `aiDemo`, `pickDemo`, `renderDemoResult`, `startDemoSel`, `dishEmoji`)
   - 중복 제거: 기존 "짝꿍 추천 칩"은 재료화면 `QUICK` 칩과 겹쳐서 → 라이브 데모로 대체. 팁 카드(`fridgeTip`)는 밀도 낮추려 홈에서 제거(함수/CSS는 남아있음). 첫 시작버튼은 "재료 여러 개로 찾기"→**"재료로 찾기"**, 라벨은 **"오늘 한끼"**.
   - **Hero 좌우 배치**(`.hero-main`): 슬로건 왼쪽 / 캐릭터 오른쪽 한 줄 → 세로 압축(274→173px). 캐릭터 영역 고정컬럼(`.hero-stage` flex 0 0 128px)으로 후광이 글자 안 가리게(캐릭터 104px, 후광 124/98). (검색: `hero-main`)
5. **🎠 인기 레시피 마퀴** — "요즘 이거 많이 만들어요" 레일 자동 무한 흐름. `renderRail()`이 카드 세트를 2번 이어붙이고 `.rail-track`을 `translateX(-50%)` 26초 무한. hover/press 시 정지, `prefers-reduced-motion`이면 수동 스크롤 폴백. (검색: `rail-track`, `railScroll`)
6. **🤔 쿡모드 "이거 맞나요?" 동행** — 각 조리 단계에 버튼 추가, 탭하면 짝꿍이 "이런 상태면 잘 되고 있는 거예요" 안심 문구. `cookCheck(text)`가 조리 동작(볶/끓/썰/조림/무침 등, 한국어 활용형 포함)을 읽어 맞춤 문구 생성(실제 60개 레시피 300단계 중 94% 맞춤). (검색: `cookCheck`, `chk-btn`, `toggleChk`)
7. **난이도 별점** — `starLevel()` → 인포바에 ⭐쉬움/⭐⭐보통/⭐⭐⭐어려움.
8. **쿡모드 조리 힌트 줄바꿈** — `hl()`의 괄호 힌트 `(중불…)`가 본문 뒤에 붙어 잘리던 걸 `.cook-text .hint{display:block}`으로 아랫줄 분리.
9. **완성 화면 홈 버튼** — `cookFinish()`에 "🏠 홈으로 돌아가기"(`.fin-home`, `goHome()`) 추가.
10. **🔐 로그인 모달** — "로그인/시작하기" → 닉네임 입력 모달(`#loginModal`, `openLogin/doLogin/closeLogin`). 입력한 닉네임으로 "○○ 님" 개인화 + 마이페이지 패널. **비밀번호/계정생성 없음**(안전상 실제 인증정보 안 다룸, 실제 Auth는 배포 단계 Supabase). 시연 영상용. (검색: `login-modal`, `openLogin`, `doLogin`)

**아직 안 한 것 (다음 우선순위):**
- 🟡 인기 레시피 "기준 명시" — HANDOFF 3장 🟡 참고. **주의: "3만명이 찾음" 같은 허구 숫자 금지.** 현재 인기 목록의 `by`는 `"레시피 · 예시"`로 표기됨. 사용자 수 주장 대신 "짝꿍이 고른 기본 메뉴" 같은 정직한 문구만.
- 🟢 백엔드/배포 (목표 B) — Supabase/Vercel 프록시, 인증, 커뮤니티 실구현. **API 키 커밋 금지.**
- (선택) 레시피북 "저장 vs 등록" 분리.

### 로컬에서 확인하는 법
AI는 claude.ai 밖에선 안 되지만 **데모 폴백으로 UI/검색은 다 확인 가능**:
```
cd <repo>
cp 한끼짝꿍_프로토타입_v35.html _preview.html   # (_preview.html은 gitignore됨)
python -m http.server 8123
# 브라우저에서 http://127.0.0.1:8123/_preview.html
```
파일을 직접 `file://`로 열어도 되지만 일부 브라우저에서 제약이 있어 로컬 서버 권장.

### 그 다음 남은 우선순위 (기존 TODO, 3장 참고)
레시피 정리(A/B/C) 후 → 🔴 홈 화면 개편(TODO 1) → 🔴 결과화면 "🤖 AI 분석" 카드(TODO 2) → 🟡 디테일 → 🟢 백엔드/배포.
